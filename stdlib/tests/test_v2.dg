fn test_v2() ~> int {
	
	v = v2_ctor(1.0, 0.0);
	w = v2_ctor(0.0, 1.0);
	
	assert(0.9 < v2_mag(v) < 1.1);
	
	assert(1 < v2_dist(v, w) < 2);
	
	v2_add(v, w);
	assert(0.9 < v.x2 < 1.1);
	
	v2_scale(w, 2.0);
	assert(1.9 < w.x2 < 2.1);
	
	v2_normalize(w);
	assert(0.9 < w.x2 < 1.1);
	
	return 0;
}
