//this file provides subroutines for dealing with
//prime numbers. 

@halts
fn isprime(uint n) -> bool {
	
	uint count = 0;

	for k in 1 .. n {
	
		if n % k == 0
		{ count++; }
	}
	return count == 2;
}

