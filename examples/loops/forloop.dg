#include <stdio.h>
#include <stdlib.h>
fn main () ~> int {
	
	int n = 4;
	
	for row in 0 .. n {
		for col in 0 .. n {
			int x = rand()%10;
			print_int(x);
			putchar('_');
		}
		puts("");
	}

	return 0;
}
