
	fn main ()~>PInt {
		putdigit(subr(id));
		putchar(' ');
		putdigit(subr(twice));

		return 0;
	}

	fn id (PInt n)->PInt {
		return n;
	}
	
	fn twice (PInt n)->PInt {return 2*n;}

	fn subr (((PInt)->PInt) subr1)->PInt {
		return subr1(2);
	}

