namespace MyStructs{
	
	struct MyStruct{
		PInt a,
		PInt b
	}


	()~>PInt main{
		MyStruct s = malloc(2); //as MyStruct has 2 members

		s.a=1; //struct member store

		putdigit(s.a); //struct member load

		return 0;
	}
}
