public namespace Array1{
	public ()~>PInt main{

		//arr=[1,2,3,4,5,6];
		[PInt] arr = new(2);
		arr[0]=0; //with only this assignment, and not the other, it works
		arr[1]=1;
		
		//putdigit(len(arr));
		printarr(arr);
		//putdigit(len(new(2)));
		return 0;	

	}

	public ([PInt] arr)~>PInt printarr{
		i=0;
		putchar('[');
		while(i < (len(arr)) ){
			//putdigit(len(arr));
			putdigit(i);
			putchar(',');
			//putdigit(arr[i]);

			i=i+1;
		}
		putchar(']');
		return 0;
	}
}
